import React from 'react';
import ReactDOM from 'react-dom';

import App from './App.jsx';

ReactDOM.render(<App />, document.getElementById('app'));
// rander is a function on reactDom (web renderer) (runs react component)
// takes two args:
//   first is react component to render, 
//   second is element on the page where you'll runder (html id)




